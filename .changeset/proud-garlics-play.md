---
"@domas/api-client": minor
"@domas/database": minor
"@domas/ts-types": minor
---

Room Change Foundation: added `room_change_requests` table, `max_room_changes` on semesters, `room_changes_count` on bookings. New `RoomChangeRequest` interfaces and DTOs in ts-types. New `portalRoomChanges` group in portal api-client and staff `roomChanges` endpoint group.
