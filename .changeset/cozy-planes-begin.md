---
"@domas/client-core": minor
"client": minor
"@domas/ui": patch
---

per-page polish. Fixed LoadingOverlay placement (inside Paper/Card, not PageShell), removed striped from all remaining tables, applied CSS module headers, replaced inline empty-state Text with EmptyState components, and used LabelValue in audit log drawer.
