---
"@domas/client-core": minor
"@domas/ts-types": minor
"@domas/ui": minor
---

Semester UI: added `maxRoomChanges` field to `CreateSemesterDto`, `SemesterModal` (NumberInput, empty = unlimited), and the semester details drawer. Added `semester.max_room_changes`, `semester.max_room_changes_hint`, and `unlimited` to EN and TR translations.
