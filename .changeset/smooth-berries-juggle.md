---
"@domas/api-client": minor
"@domas/ts-types": minor
---

Added `StudentApplication` interface and `SubmitApplicationDto` to ts-types. Added `portalApplications` client (`submit`, `getStatus`) for the public registration flow, and `listApplications`, `reviewApplication`, `getApplicationLetterUrl` to the staff `students` client.
