---
"client": minor
---

Rector role : rector pages

- `RectorOverviewPage` — all KPIs in grouped stat cards (residents, students, finances, issues); `SimpleGrid cols={{ base: 2, sm: 4 }}` for mobile-first layout
- `RectorOccupancyPage` — occupancy rate ring chart + breakdown stat cards
- `RectorFinancesPage` — payment summary cards + overdue progress bar
- `RectorIssuesPage` — pending damages, room changes, and approval counts
- `RectorProfilePage` — thin wrapper around `SharedAccountPage`
